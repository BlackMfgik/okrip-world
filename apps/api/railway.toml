[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
[deploy]
preDeployCommand = "pnpm --filter @okrip/api db:migrate"
healthcheckPath = "/ready"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
